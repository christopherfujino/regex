#include "regex.hpp"

// Inductive operators
// Union R = S | T (lowest precedence)
// Concatenation R = S ⋅ T
// Closure R = S* (highest precedence)

Expr parse(std::vector<Token>) {
  throw std::exception();
}
